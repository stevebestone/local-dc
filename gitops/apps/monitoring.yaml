apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: monitoring
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://prometheus-community.github.io/helm-charts
    chart: kube-prometheus-stack
    targetRevision: 72.*
    helm:
      values: |
        grafana:
          adminUser: admin
          adminPassword: ""
          existingSecret: grafana-admin
          grafana.ini:
            server:
              root_url: http://localhost:30084
            auth:
              disable_login_form: false
            auth.generic_oauth:
              enabled: true
              name: Keycloak
              allow_sign_up: true
              client_id: grafana
              client_secret: grafana-secret-change-me
              scopes: openid profile email groups
              auth_url: http://localhost:30083/realms/local-dc/protocol/openid-connect/auth
              token_url: http://localhost:30083/realms/local-dc/protocol/openid-connect/token
              api_url: http://localhost:30083/realms/local-dc/protocol/openid-connect/userinfo
              role_attribute_path: contains(groups[*], 'admins') && 'Admin' || 'Viewer'
          service:
            type: NodePort
            nodePort: 30084
          sidecar:
            dashboards:
              enabled: true
          dashboardProviders:
            dashboardproviders.yaml:
              apiVersion: 1
              providers:
                - name: default
                  folder: ''
                  type: file
                  options:
                    path: /var/lib/grafana/dashboards/default
        prometheus:
          prometheusSpec:
            retention: 14d
            resources:
              requests:
                memory: 1Gi
                cpu: 500m
              limits:
                memory: 4Gi
                cpu: 2000m
            storageSpec:
              volumeClaimTemplate:
                spec:
                  accessModes: ["ReadWriteOnce"]
                  resources:
                    requests:
                      storage: 50Gi
        alertmanager:
          enabled: false
        nodeExporter:
          enabled: true
        kubeStateMetrics:
          enabled: true
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
